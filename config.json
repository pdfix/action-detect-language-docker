{
  "name": "Language Detection",
  "desc": "Action running Language Detection.",
  "version": {
    "major": 1,
    "minor": 0
  },
  "actions": [
    {
      "name": "Language detection",
      "desc": "Detecting language of given document or text.",
      "category": "Accessibility",
      "stdout": "",
      "stderr": "",
      "returnCodes" : [0],
      "configurations": [
        {
          "program": "docker run -v ${WORKING_DIRECTORY}:/data -w /data --rm pdfix/lang-detect:latest lang-detect -i $(basename ${INPUT_PDF}) -o $(basename ${OUTPUT_PDF})",
          "args": [
            "--input",
            "${INPUT_PDF}",
            "--output",
            "${OUTPUT_PDF}"
          ],
          "name": "Detect document language and set it to the document",
          "desc": "Detecting language of given document and save it into output document."
        },
        {
          "program": "docker run -v ${WORKING_DIRECTORY}:/data -w /data --rm pdfix/lang-detect:latest lang-detect -i $(basename ${INPUT_PDF}) -o $(basename ${OUTPUT_TXT})",
          "args": [
            "--input",
            "${INPUT_PDF}",
            "--output",
            "${OUTPUT_TXT}"
          ],
          "name": "Detect document language",
          "desc": "Detecting language of given document and save it into output TXT file."
        }        
      ],
      "args": [
        {
          "name": "INPUT_PDF",
          "desc": "Input PDF file",
          "flags": 2,
          "ext": "pdf",
          "value": ""
        },
        {
          "name": "OUPTUT_PDF",
          "desc": "Output PDF file.",
          "flags": 4,
          "ext": "pdf",
          "value": ""
        },
        {
          "name": "OUTPUT_TXT",
          "desc": "Output text file with result of detection.",
          "flags": 4,
          "ext": "txt",
          "value": ""
        }        
      ]
    }
  ]
}