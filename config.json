{
  "name": "Pipeline Language Detection",
  "desc": "Pipeline running Language Detection command.",
  "version": {
    "major": 1,
    "minor": 0
  },
  "pipeline": [
    {
      "name": "Language detection",
      "cmd": "lang_detect",
      "desc": "Detecting language of given document or text.",
      "path": "user-defined command folder",
      "program": "lang_detect",
      "configurations": [
        {
          "args": [
            "--input",
            "${Input PDF}",
            "--output",
            "${Output PDF}"
          ],
          "name": "Detect document language and set it to the document",
          "desc": "Detecting language of given document and save it into output document."
        },
        {
          "args": [
            "--input",
            "${Input PDF}",
            "--output",
            "${Output TXT}"
          ],
          "name": "Detect document language",
          "desc": "Detecting language of given document and save it into output TXT file."
        },
        {
          "args": [
            "--input",
            "${Input Text}",
            "--output",
            "${Output TXT}"
          ],
          "name": "Detect text language",
          "desc": "Detecting language of given text and save it into output TXT file."
        }
      ],
      "configuration": {
        "args": [
          "--input",
          "${Input PDF}",
          "--output",
          "${Output PDF}"
        ],
        "name": "Detect document language and set it to the document",
        "desc": "Detecting language of given document and save it into output document."
      },
      "args": [
        {
          "name": "Input PDF",
          "desc": "Input PDF file",
          "type": "file_path",
          "value": "user-defined file path"
        },
        {
          "name": "Output PDF",
          "desc": "Output PDF file.",
          "type": "file_path_save",
          "value": "user-defined file path"
        },
        {
          "name": "Input Text",
          "desc": "Text used to detect language",
          "type": "string",
          "value": "text for language detection"
        },
        {
          "name": "Output TXT",
          "desc": "Output TXT file where is result of detection saved.",
          "type": "file_path_save",
          "value": "user-defined file path"
        }
      ]
    }
  ]
}